@model BookStore.Models.GenreBook

@{
    ViewData["Title"] = "Genres";
}

<h1>TÜRLER</h1>
<form asp-action="Genres_son">
    <div class="form-group">
        
        <select asp-for="GenreId" class="form-control" asp-route-genreId="GenreId" asp-items="@(IEnumerable<SelectListItem>)ViewBag.Genres"></select>
        <span asp-validation-for="GenreId" asp-route-genreId="GenreId" class="text-danger"></span>
    </div>

    <div class="form-group">
        <input type="submit" value="Ara" class="btn btn-success" />
    </div>
</form>