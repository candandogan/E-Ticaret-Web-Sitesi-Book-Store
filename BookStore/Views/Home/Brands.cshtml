@model BookStore.Models.Brand

@{
    ViewData["Title"] = "Brands";
}

<h1>YAYINEVLERİ</h1>
<form asp-action="Brands_son">
    <div class="form-group">
        @*<label asp-for="BrandId" class="control-label"></label>*@
        <select asp-for="BrandId" class="form-control" asp-route-brandId="BrandId" asp-items="@(IEnumerable<SelectListItem>)ViewBag.Brands"></select>
        <span asp-validation-for="BrandId"  asp-route-brandId="BrandId" class="text-danger"></span>
    </div>

    <div class="form-group">
        <input type="submit" value="Ara" class="btn btn-success" />
    </div>
</form>
  